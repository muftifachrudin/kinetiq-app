# For local dev (`pip install -r` this file to work on ingestion alone).
# The Coolify-deployed ingestion worker (apps/products/trading/ingestion/
# Dockerfile) installs from this same file directly.
ccxt>=4.4
requests>=2.31
sqlalchemy>=2.0
psycopg[binary]>=3.1
