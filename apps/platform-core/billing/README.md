# Platform Core: Billing

Stripe webhook handler + subscription state sync ke Neon (tabel `tenant`), di-model per product+tier (mis. `trading:signal_only`, `trading:auto_execute`).

See `docs/prd.md` (PRD + Rencana Teknis: Kinetiq) for full context and design decisions.
