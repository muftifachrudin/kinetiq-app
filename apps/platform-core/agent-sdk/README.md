# Platform Core: Agent SDK

Kontrak dasar (`BaseAgentGraph`) LangGraph yang diturunkan tiap graph vertical — node standar ground->plan->execute->validate->deliver, auto-attach Langfuse callback, auto-resolve llm_config. Lihat PRD Section C.1.

See `docs/prd.md` (PRD + Rencana Teknis: Kinetiq) for full context and design decisions.
